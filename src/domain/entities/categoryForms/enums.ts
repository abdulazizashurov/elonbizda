export enum FormType {
  BOOLEAN = 'boolean',
  REACH_TEXT = 'reach-text',
  CHARACTER = 'character',
  DATE = 'Date',
  DATE_TIME = 'date-time',
  IMAE = 'Image',
  ENUM = 'enum',
  NUMERIC = 'numeric',
  URL = 'url',
  COVER_IMAE = 'cover-image',
  NUMERIC_ARRAY = 'numberic-array',
  CHARACTER_ARRAY = 'character-array'
}

export enum FormElement {
  NONE = 'none',
  AUTO_COMPLETE = 'auto-complete',
  TEXTAREA = 'textarea',
  CHARACTER = 'character',
  CHEXBOX = 'chexbox',
  RADIO = 'radio',
  SELECT = 'select',
  BUTTON = 'button',
  ENUM_BUTTON = 'enum-button'
}
